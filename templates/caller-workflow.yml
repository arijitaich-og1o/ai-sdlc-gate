# OPTIONAL fast path. Every repository is already gated centrally by the Organisation Gate; this caller only makes
# results appear seconds after a push instead of minutes. Copy to .github/workflows/sdlc-gate.yml if wanted.
#
# Requirements
#   - Organisation secrets LITELLM_API_KEY, LITELLM_BASE_URL and SDLC_GATE_TOKEN shared with the repository
#     (secrets: inherit below forwards them). If your org uses different names, map them explicitly.
#   - The organisation ruleset requires the "SDLC Gate" commit status; this caller's check ("gate / SDLC Gate") is
#     recognised by the Organisation Gate, which then skips its own review of the same commit.
#
# Pin `@main` to a release tag or commit SHA if your team wants to control when policy updates land.
name: SDLC Gate

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - develop
      - "release/**"
      - "hotfix/**"

permissions: {}

concurrency:
  group: sdlc-gate-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    permissions:
      contents: read
      pull-requests: write
    uses: arijitaich-og1o/ai-sdlc-gate/.github/workflows/sdlc-gate.yml@main
    with:
      gate-ref: main
      # intent: deploy        # optional: force phases 3-7 for this workflow (defaults to auto-detection)
    secrets: inherit
